<Window x:Class="OverlayPlayer.GiphySearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        Title="Giphy Search" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Name="TitleText" Text="Giphy Search" FontSize="24" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>
                <Button Content="✕" Click="Close_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" FontSize="20" Foreground="#999999" Cursor="Hand" Padding="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#FF0000"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </DockPanel>

            <!-- Search Bar -->
            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Name="SearchBox" Grid.Column="0" Height="40" VerticalContentAlignment="Center" Padding="10,0" FontSize="14" KeyDown="SearchBox_KeyDown"
                         BorderBrush="#E0E0E0" BorderThickness="1">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <Button Name="SearchButton" Grid.Column="1" Content="Search" Width="100" Height="40" Margin="10,0,0,0" Click="SearchButton_Click"
                        Background="#007AFF" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <CheckBox Name="StickerCheckBox" Grid.Column="2" Content="Stickers" VerticalAlignment="Center" Margin="10,0,0,0" Checked="StickerCheckBox_CheckedChanged" Unchecked="StickerCheckBox_CheckedChanged">
                    <CheckBox.LayoutTransform>
                        <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                    </CheckBox.LayoutTransform>
                </CheckBox>
            </Grid>

            <!-- Result Grid -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <WrapPanel Name="ResultsPanel" HorizontalAlignment="Center" />
            </ScrollViewer>

            <TextBlock Name="LoadingText" Grid.Row="2" Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Visibility="Collapsed" Foreground="#666666" FontSize="16"/>
        </Grid>
    </Border>
</Window>
